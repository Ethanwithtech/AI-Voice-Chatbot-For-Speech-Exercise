# 2026-03-14 工作日志

## 新注册/登录流程实现
- **后端**: 
  - `backend/app/models/user.py` — 添加 `StudentRegisterInput`、`TeacherAccessRequestInput` Pydantic模型，UserResponse添加section字段
  - `backend/app/database.py` — User模型添加 `section` 和 `is_approved` 字段，_run_migrations添加迁移
  - `backend/app/auth.py` — user_to_dict 添加 section 和 is_approved
  - `backend/app/routers/auth_router.py` — 新增端点：
    - `POST /student-register` — 学生3步注册（学号后4位+姓名缩写+section→生成唯一ID如6342-YD-A1）
    - `POST /teacher-request-access` — 教师申请访问（is_approved=False，需管理员审批）
    - `PUT /approve-teacher/{user_id}` — 管理员审批教师
    - `GET /pending-teachers` — 获取待审批教师列表
    - 教师登录增加is_approved检查

- **前端**:
  - `frontend/src/lib/auth.tsx` — 添加 `registerStudent()` 和 `requestTeacherAccess()` 方法
  - `frontend/src/pages/LoginPage.tsx` — 完全重写为多步流程：
    - Welcome → 选择身份(Student/Teacher)
    - Student → Welcome back(登录) / I'm new here(注册)
    - 3步注册: 学号后4位 → 姓名缩写 → Section → 生成唯一ID
    - Teacher → Sign In / Request Access tabs

## 练习材料可见性
- `frontend/src/pages/PracticePage.tsx` — 录音阶段显示exercise的reference_text和description
- `frontend/src/pages/CRAAPracticePage.tsx` — 录音阶段添加可折叠的"View Materials"面板(key claim, argument text, topic context)，准备阶段添加可折叠的argument text

## CRAA笔记和录音播放
- Listen阶段笔记区：改大textarea（h-40），添加引导placeholder（Context, Claim, Evidence格式）
- Listen按钮文案改为 "I'm Ready — Start Preparation Time"（匹配Replit截图）
- 结果页添加"Your Recording"播放按钮，通过 `/api/practice/session/{id}/audio` 加载录音
- 播放状态管理：playbackAudioRef, isPlayingResult, togglePlayback()

## 切换到 faster-whisper
- `backend/requirements.txt` — 移除 openai-whisper+torch (739MB)，改用 faster-whisper (60MB)
- `backend/app/services/whisper_local_service.py` — 完全重写使用 faster-whisper (CTranslate2引擎)
- 模型: **Whisper base** (默认)，可通过 `WHISPER_MODEL_SIZE` 环境变量调整
- compute_type: **int8** (默认)，通过 `WHISPER_COMPUTE_TYPE` 环境变量调整
- 启用了 VAD filter 减少误识别

## 推送
- 已推送到 GitHub main 分支 (commit 250aad5)

## Replit合并后重新实现（第二次推送）
- Replit用 `git pull --no-rebase` 合并时覆盖了我们的代码，所有注册/登录功能丢失
- 在Replit合并后的代码基础上重新实现了全部注册/登录功能（保留了Replit的改进: IntroCarousel、PostgreSQL pool config、CountdownRing等）
- 重写文件: user.py, database.py, auth.py, auth_router.py, auth.tsx, LoginPage.tsx
- CRAAPracticePage.tsx: 在Replit版本上补回4个缺失功能:
  1. Listen阶段笔记区（textarea h-40，引导placeholder）
  2. Prepare阶段可折叠argument text + 笔记编辑区
  3. Record阶段可折叠View Materials面板（key claim, argument, topic context）+ 笔记显示
  4. Result页录音播放（loadPlaybackAudio via session_id, togglePlayback）
- Try Again按钮中增加了notes/playback/audioStatus的重置逻辑
